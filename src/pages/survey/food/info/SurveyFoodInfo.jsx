import React from 'react';

const SurveyFoodInfo = () => {
  // 태그를 클릭했다면, context []에 값을 추가,
  // 태그를 해제했다면 context 값을 제거한다.
  return (
    <div>
      메뉴 분류
    </div>
  );
};

export default SurveyFoodInfo;